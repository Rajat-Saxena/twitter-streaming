<bundle-app name='twitter-streaming' xmlns='uri:oozie:bundle:0.2'> 
	<controls>
		<kick-off-time>${startTime}</kick-off-time>
	</controls>

	<coordinator name='kafka-coordinator' >
		<app-path>${KAFKA_COORDINATOR}</app-path>
	</coordinator>

	<coordinator name='locations-coordinator' >
		<app-path>${LOCATIONS_COORDINATOR}</app-path>
	</coordinator>

</bundle-app> 